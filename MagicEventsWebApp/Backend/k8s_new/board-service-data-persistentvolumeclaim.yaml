apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: board-service-data
  name: board-service-data
  namespace: magicevents
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
