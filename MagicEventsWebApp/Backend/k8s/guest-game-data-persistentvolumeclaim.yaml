apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: guest-game-data
  name: guest-game-data
  namespace: magicevents
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
