apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: events-management-data
  name: events-management-data
  namespace: magicevents
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
