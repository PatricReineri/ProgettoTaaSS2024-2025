apiVersion: v1
kind: Service
metadata:
  name: postgres-user-management-DB
  namespace: magicevents
spec:
  selector:
    app: user-management-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP